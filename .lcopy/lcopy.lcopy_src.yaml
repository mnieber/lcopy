# Configuration files to use (can list multiple)
sources:
  ..: lcopy_src

targets:
  default:
    include:
      - lcopy_src.copy-code

options:
  destination: ~/projects/lcopy/res/claude/lcopy_src
  concatenated_output_filename: ~/projects/lcopy/res/claude/input/lcopy_src.txt
  conflict: overwrite
  verbose: false
  purge: true
